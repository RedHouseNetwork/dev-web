#!/bin/bash
SCRIPT_DIR="$(cd "$(dirname "$0")/.." && pwd)"
source "$SCRIPT_DIR/.env"
WORKDIR="${PWD/#$WEB_ROOT//web}"
if [[ "$WORKDIR" == /web* ]]; then
    docker exec -it -w "$WORKDIR" mysql /bin/bash
else
    docker exec -it mysql /bin/bash
fi
