#!/bin/bash
SCRIPT_DIR="$(cd "$(dirname "$0")/.." && pwd)"
source "$SCRIPT_DIR/.env"
if [[ "$PWD" == "$WEB_ROOT"* ]]; then
    docker exec -it -w "$PWD" php84 bash
else
    docker exec -it php84 bash
fi
