#!/bin/bash
SCRIPT_DIR="$(cd "$(dirname "$0")/.." && pwd)"
source "$SCRIPT_DIR/.env"
if [[ "$PWD" == "$WEB_ROOT"* ]]; then
    docker exec -it -w "$PWD" php83 bash
else
    docker exec -it php83 bash
fi
